- var base='gear'

.component(class=base)
  h3(data-i18n='gear') Gear
  ul(class=base+'-header')
    +repeating-li(base, 'Item', 'name', 'item')
    +repeating-li(base, 'Rating', 'rating')
  fieldset(class='repeating_'+base)
    +add-repeating-inputs(base, ['item', 'rating'])